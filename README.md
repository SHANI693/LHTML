If we create folder inside folder both are .git repository then its cause many problem
<br>
we can check which forlder is .git or not by using get-childitem -force
<br>
Remove-Item -Recurse -Force .\LHTML\.git is commmand use for deleting .git
<br>
git remote show origin is command use for show origin
